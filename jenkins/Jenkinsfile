node {
  checkout scm
  
  echo "Loading varPipeline.groovy"
  def varPipeline = load 'jenkins/varPipeline.groovy'
  echo "varPipeline loaded: ${varPipeline}"

  varPipeline {
    name = 'Brendan'
    age = '22'
  }
}

