
node {
    // Load the greetingsPipeline.groovy script
    def greetingsPipeline = load 'jenkins/greetingsPipeline.groovy'

    // Call the greetingsPipeline function
    greetingsPipeline {
        name = 'World' // Pass parameters to the pipeline
    }
}
