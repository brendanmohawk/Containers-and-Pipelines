// Jenkinsfile
pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                checkout scm
                 sh 'pwd'
                 sh 'ls -alh'
            }
        }
    }
}
